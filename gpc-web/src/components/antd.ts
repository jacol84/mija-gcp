import {Button as AButton, Form as AForm, Input as AInput} from 'ant-design-vue';

const AFormItem = AForm.Item
const AButtonGroup = AButton.Group
export {AButton, AForm, AInput, AFormItem, AButtonGroup}